apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

namePrefix: prod-

namespace: prod

patchesJson6902:
- target:
    group: v1
    version: v1
    kind: Service
    name: hello
  patch: |-
    - op: replace
      path: /spec/ports/0/port
      value: 8053

configMapGenerator:
- name: hello-config
  literals:
  - environment=prod
  - hello=Hello from Kubernetes!
