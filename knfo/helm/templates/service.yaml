apiVersion: v1
kind: Service
metadata:
  name: {{ include "knfo.fullname" . }}
  labels:
    {{- include "knfo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "knfo.selectorLabels" . | nindent 4 }}
