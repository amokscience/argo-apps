{{- if .Values.monitoring.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "ntest.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ntest.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "ntest.selectorLabels" . | nindent 6 }}
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
{{- end }}
